# .github/

The workflow notes are the human-facing `docs/ci-and-release.md`, and the engine builds `engines.yml` drives are
`docs/engines.md`; both are imported below. Edit them there, not here.

@../docs/ci-and-release.md
@../docs/engines.md
