# native/

The engine build scripts and their gates. The notes are the human-facing `docs/engines.md`, imported below; edit
them there, not here. How CI drives these scripts, and what its cache keys hash, is in `docs/ci-and-release.md`.

@../docs/engines.md
